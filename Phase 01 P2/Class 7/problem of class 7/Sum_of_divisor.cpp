#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

void solve() {
    // I will rely on these theme // I will be the one you rely on a 
    int n; cin >> n;

    for (int i = 1; i <= n; i++) {
        cout << i << " ";
    }

}

int32_t main() {   
    ios_base :: sync_with_stdio(0);
    cin.tie(0);
    
    int t = 1; cin >> t;
    while(t--) solve();
    return 0;
} 